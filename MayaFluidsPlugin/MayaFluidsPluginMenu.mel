if (!`menu -exists fluidsPluginMenu`)
{
    menu -parent MayaWindow 
         -label "Fluids Plugin" 
         -tearOff false
         fluidsPluginMenu;
    menuItem -label "Generate"
             -command "generateFluid";
    menuItem -label "Editor..."
             -command "createFluidUI";
}
